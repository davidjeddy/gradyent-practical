apiVersion: v1
kind: Service
metadata:
  namespace: web-app
  name: service-web-app
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: web-app
