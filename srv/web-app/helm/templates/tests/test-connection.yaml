apiVersion: v1
kind: Pod
metadata:
  name: {{ .Chart.Name }}-test-connection
  labels:
    app.kubernetes.io/name: app-{{ .Chart.Name }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: {{ .Chart.Name }}:{{ .Values.targetPort }}
  restartPolicy: Never
